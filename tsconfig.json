{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "rootDir": "./",
        "outDir": "./dist",
        /* added types from @types */
        "lib": ["ESNext", "DOM", "DOM.Iterable"],
        "types": ["bun", "vite/client", "unplugin-turbo-console/client"],
        /* Bundler mode */
        "moduleResolution": "bundler",
        /* transpilation settings */
        "module": "esnext",
        "target": "esnext",
        /* `noEmit` is required because we want to compile the TS file into JS. */
        "noEmit": true,
        /* Bundler mode */
        "moduleDetection": "force",
        /* Path aliases */
        "paths": {
            "@/*": ["./*"],
            "@app/*": ["./launcher/*"],
            "@src/*": ["./src/*"],
            "@lib/*": ["./src/lib/*"],
            "@components/*": ["./src/components/*"],
            "@app-lib/*": ["./launcher/lib/*"],
            "@src-lib/*": ["./src/lib/*"]
        },
        /* JSX/TSX support */
        "jsx": "preserve", // keep the JSX syntax so that `jsx-dom` can convert it.
        "jsxImportSource": "jsx-dom" // imports the types, functions, etc
    },
    "files": ["./vite.config.ts", "./vitest.config.ts"],
    "include": ["**/*.ts", "**/*.tsx"],
    "exclude": ["dist", "node_modules"]
}
